spring:
    application:
      name: MedicalAPP
    datasource:
      url: jdbc:mysql://localhost/medicalEPI?createDatabaseIfNotExist=true&serverTimezone=UTC
      username: root
      password:
    jpa:
      hibernate:
        ddl-auto: update
      show-sql: true
server:
  port: 8090